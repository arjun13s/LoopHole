Repository root is the current working directory. Task:
Fix the failing date range utility. The repo already has code and tests. Diagnose the failure, patch the local logic bug, rerun tests, then submit.

Files available:
- repo/README.md
- repo/src/date_ranges.py
- repo/src/formatting.py
- repo/tests/test_date_ranges.py

Use `pytest -q` for tests. If you submit, include exactly one of:
["repo/src/date_ranges.py"]

Keep scope narrow. Expected repo size is small; prefer targeted reads.
